# Databricks Asset Bundle for genai_notebooks
# Deploy: databricks bundle deploy -t <target>
# Run PII profiling: databricks bundle run -t <target> pii_profiling_job

bundle:
  name: genai_notebooks

include:
  - resources/*.yml

targets:
  dev:
    mode: development
    default: true
  prod:
    mode: production
